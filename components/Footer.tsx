export default function Footer() {
  return (
    <footer className="pt-[calc(var(--gap)*2.5)] font-mono text-meta text-muted">
      <p>© {new Date().getFullYear()} mara masaeva</p>
    </footer>
  )
}
